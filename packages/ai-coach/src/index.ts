/**
 * AI Learning Coach Agent
 * 학생 맞춤형 AI 학습 코치
 *
 * 현재 구현은 questyCoachAgent 패키지에 있습니다.
 * 이 패키지는 추후 통합을 위한 스켈레톤입니다.
 */

export const AI_COACH_VERSION = '0.1.0';

// 추후 questyCoachAgent에서 이관될 기능들
export interface AICoachConfig {
  // 설정 옵션
}

export class AICoach {
  constructor(_config?: AICoachConfig) {
    // 초기화
  }
}
